#include<stdio.h>
#include<math.h>

int main(){
    float x, y, z, a;
    scanf("%f",&x);
    scanf("%f",&y);
    scanf("%f",&z);
    a =pow(x,2)+pow(y,2)+pow(z,2);
    printf("%f",a);
    return 0;






}